Glob = function () {}

Glob.canvas = null;
Glob.width = null;
Glob.height = null;

Glob.sx = 0; // shift x coordinate
Glob.sy = 0; // shift y coordinate
Glob.fx = 1; // factor(multiply) x coordinate
Glob.fy = 1; // factor(multiply) y coordinate

// channels
Glob.chR = 1; // channel red
Glob.chG = 1; // channel red
Glob.chB = 1; // channel red
